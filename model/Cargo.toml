[package]
name = "model"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8.5"
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"
uuid = { version = "1.8.0", features = ["v4", "fast-rng", "serde", "macro-diagnostics"] }
